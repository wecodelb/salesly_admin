# The API this console talks to. The hosted server by default; override with a
# local `artisan serve` (http://localhost:8000/api/v1) when developing.
VITE_API_URL=https://salesly.braindevsolutions.com/api/v1
